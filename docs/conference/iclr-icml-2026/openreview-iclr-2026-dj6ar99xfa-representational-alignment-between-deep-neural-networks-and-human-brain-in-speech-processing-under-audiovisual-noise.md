---
title: Representational Alignment between Deep Neural Networks and Human Brain in Speech Processing under Audiovisual Noise
title_zh: 视听噪声下深度神经网络与人脑语音处理的表征对齐
authors: "Cheng Luo, Jian Zhu, Zhoujian Sun, Yun-Yi Qi, Wenhui Sun, Wenyuan Yu"
date: 2025-09-19
pdf: "https://openreview.net/pdf?id=DJ6AR99XFA"
tags: ["query:eeg-speech"]
score: 6.0
evidence: 用神经编码模型和RSA研究语音处理中DNN-脑表征对齐，对EEG与语音多模态对齐有方法借鉴
tldr: 论文研究语音识别过程里DNN层级表征与人脑从声学到语言处理的对齐关系。作者用神经编码模型分别模拟声学包络、噪声包络和词起始等特征诱发的神经响应，再用表征相似性分析量化DNN与脑区表征的相似度。结果表明模型层级与听觉语言通路存在可量化的层级对应，为EEG语音多模态特征对齐提供编码模型加RSA的通用分析范式。
source: ICLR-2026-Public
selection_source: conference_retrieval
motivation: 语音识别中DNN层级如何对人脑声学到语言处理层级尚未被精确刻画。
method: 使用神经编码模型模拟声学与语言特征反应，并利用表征相似性分析量化DNN脑表征对齐。
result: 发现DNN表征与脑处理层级在声学到语言维度存在结构对应关系。
conclusion: 编码模型与RSA结合可用于语音神经表征对齐，为EEG与语音多模态分析提供方法基础。
---

## Abstract
Speech recognition in the human brain is an incremental process that begins with acoustic processing and advances to linguistic processing. While recent studies have revealed that the hierarchy of deep neural networks (DNNs) correlates with the ascending auditory pathway, the exact nature of this DNN-brain alignment remains underexplored. In this study, we investigate how DNN representations align with the brain's acoustic-to-linguistic processing. Specifically, we employed neural encoding models to simulate neural responses to acoustic (i.e., speech and noise envelope) and linguistic features (i.e., word onset and surprisal). By applying representational similarity analysis (RSA), we quantified the similarity between these neural responses and the DNN embeddings generated by a pre-trained automatic speech recognition (ASR) model, both before and after fine-tuning on audiovisual noisy data. Our results demonstrate significant DNN-brain alignment:  embeddings from shallow layers exhibit higher similarity to neural responses associated with acoustic features, while those from deeper layers align more closely with neural responses related to linguistic features. Importantly, the fine-tuning process enhances this alignment by improving noise processing in shallower layers and refining linguistic representations in deeper layers. These results suggest that fine-tuned DNN models can naturally develop human-like processing patterns in noisy environments, highlighting a functional alignment between the human brain and DNNs in speech representation.

---

## 论文详细总结（自动生成）

## 1. 核心问题与整体含义

- **研究动机**：人脑的语音识别是一个从声学处理逐步过渡到语言处理的增量过程，而深度神经网络（DNN）的层级结构也被发现与大脑的上行听觉通路存在相关性。然而，这种“DNN与大脑”的对齐关系究竟在声学至语言的连续维度上如何具体体现，仍缺乏精确刻画。
- **核心问题**：DNN 的逐层表征是否以及在何种程度上与大脑在语音处理中的声学-语言层级表征相对应？在多模态噪声环境下，这种对齐如何随模型微调变化？
- **整体含义**：该研究不仅揭示了神经形态的层级处理同构性，还表明经过视听噪声微调的 ASR 模型能自发形成类似人脑的噪声鲁棒性表征，为类脑语音处理模型设计提供了证据。

## 2. 方法论

- **核心思想**：将神经编码模型与表征相似性分析（RSA）结合，用于量化 DNN 内部表征与人脑神经响应之间的相似性，从而实现跨物种、跨模态的层级对齐度量。
- **关键技术细节**：
  - 特征模拟：针对声学特征（语音包络 `speech envelope`、噪声包络 `noise envelope`）和语言特征（词起始 `word onset`、词预期度/惊讶度 `surprisal`）分别使用神经编码模型拟合对应的脑电反应。
  - DNN 表征：使用预训练自动语音识别（ASR）模型的隐层嵌入作为 DNN 侧表征源，并对比在视听噪声数据上微调前后的嵌入。
  - 对齐度量：通过 RSA 计算每层 DNN 嵌入与不同神经编码模型预测响应之间的相似性矩阵，从而映射 DNN 各层与大脑声学/语言加工阶段之间的对应程度。
- **算法流程**（文字说明）：
  1. 采集人脑处理语音（含噪声）时的神经数据；
  2. 用编码模型分别对声学包络、噪声包络、词起始和惊讶度等特征进行神经响应预测；
  3. 提取 ASR 模型中间各层嵌入；
  4. 对原始模型与微调后模型分别计算 RSA 相似度；
  5. 比较浅层/深层嵌入与不同特征响应的匹配度，得出层级对齐图谱。

## 3. 实验设计

- **数据集/场景**：论文描述中提及使用“视听噪声数据”（audiovisual noisy data）进行微调，但没有明确具体数据库名称（如是否使用音视频语音数据库、EEG 数据集等），也未给出数据规模。
- **Benchmark**：没有与外部方法进行横向对比，实验目标集中在验证 DNN-大脑对齐的存在性与微调带来的变化，因此更像一种效应检验与机制分析，而非标准 benchmark 竞赛。
- **对比方法**：
  - 对比了微调前后同一 ASR 模型的行为差异（作为自身对照）；
  - 比较了浅层与深层嵌入分别与声学特征响应和语言特征响应的相似度差异。

## 4. 资源与算力

- 论文提取内容中**未明确说明**使用的 GPU 型号、数量、训练时长、内存占用等资源信息。
- 只能推测属于中小规模实验（EEG 数据量通常有限，ASR 微调一般使用单卡/双卡即可完成），但原文没有给出任何可验证的细节。

## 5. 实验数量与充分性

- **实验组数**：摘要中至少包含一组主实验（DNN 层级与脑响应 RSA），以及一组对比条件（微调前 vs 微调后）。
- **充分性评估**：
  - 亮点：能够同时覆盖声学（包络）与语言（词起始、惊讶度）特征，形成两个层级的对照，逻辑自洽。
  - 不足：属于单数据库、单模型、单模态（EEG）的验证，缺少跨数据、跨模型架构、跨模态（如 fMRI/MEG）的重复，也缺少统计显著性、效应量等详细结果。因此实验规模较为有限，结论普适性需要更多支持。

## 6. 主要结论与发现

- DNN 浅层嵌入与声学特征的神经响应相似度更高，深层嵌入与语言特征的神经响应相似度更高，证明 DNN 层级结构与大脑“声学→语言”处理路径存在**量的层级对应关系**。
- 微调过程增强了这种对齐：浅层改善噪声处理表示，深层优化语言表示，最终使 DNN 在噪声条件下展现出更接近人脑的功能组织规律。

## 7. 优点

- **跨层次统一度量**：同时处理声学与语言特征，用一个连续框架（层号→RSA相似度）呈现声学到语言的映射，直观且便于解释。
- **方法可扩展**：编码模型 + RSA 的组合不依赖特定 DNN 或特定脑成像模态，可作为通用分析范式迁移到 EEG 语音多模态对齐等多个任务。
- **机制洞察**：通过微调前后对比，展示了训练过程如何促使模型在表征结构层面“类脑化”，比单纯精度提升更深入一层。

## 8. 不足与局限

- **信息缺失**：原文只提供了摘要级信息，实验数据细节、预处理、统计检验、模型结构、微调设置均未呈现，限制了可复现性和客观评估。
- **单模型/单数据库**：没有多个 DNN 架构（如 transformer 以外的 CNN/RNN）对比，也没有验证是否与不同参数规模或训练目标有关。
- **神经数据范围**：EEG 时间分辨率高但空间分辨率低，对“脑区”或“通路”的层级描绘存在大体上粗粒度的局限；若要更准确定位声学到语言层级，需要结合 fMRI/MEG 或颅内记录交叉验证。
- **鲁棒性分析缺失**：没有给出 RSA 结果的置信区间或显著性检验信息，无法判断效应差异是否稳健。
- **应用限制**：结论基于特定噪声类型（视听噪声）微调，未覆盖其他退化情况，可能限制其向现实复杂听觉场景迁移的有效性。

（完）
